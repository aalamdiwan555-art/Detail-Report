<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:contentDescription="ULTRA detection overlay">

    <FrameLayout
        android:id="@+id/pushRoot"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:alpha="0.9"
        android:background="@drawable/bg_push_circle"
        android:clickable="true"
        android:contentDescription="Push detected action"
        android:elevation="12dp">

        <TextView
            android:id="@+id/tvPush"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="PUSH"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold" />
    </FrameLayout>

    <ImageButton
        android:id="@+id/btnCloseOverlay"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="0dp"
        android:layout_marginEnd="0dp"
        android:background="@drawable/bg_close_circle"
        android:contentDescription="Stop detection"
        android:elevation="14dp"
        android:padding="4dp"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:tint="@android:color/white" />
</FrameLayout>